# inject

## 需求 / 功能

1. 从 request HEAD 获取信息， 比如 Auth
2. 从 BODY 解析信息并获取信息，比如 JSON
3. 验证信息， 是否存在、是否合格， 比如 验证验证码

## 蓝图

type view struct {
	UserID RequireAuth
	Body struct{
	    JSON
		Password string
	}
}

type view struct {
	isAdmin struct{
        GroupAdmin
        Level GroupAdminExtLevel
    }
	Body struct{
	    JSON
		Password string
	}
}

只允许嵌套一层

嵌套匿名结构体功能
    1  将整个匿名结构体作为 plugin 处理
    2  结构体下属插件列表作为 group plugin 的扩展