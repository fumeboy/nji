type get_query_params struct {
	nji.Route[route.GET, route.ROOT]

	A plugins.QueryParam[schema.Must]
	B plugins.QueryParam[struct {
		schema.Must
		schema.IsPhoneNumber
	}]
    C plugins.QueryParam[schema.Must[schema.IsPhoneNumber[any]]]
}

type A struct{
    Embed[Embed2[Embed3[Embed4[Embed5]]]]
    Func[func(schema.Must,schema.IsPhoneNumber)]
}

type B struct{
    
    A typ.TEXT
    B typ.VARCHAR
}